<div class="container my-5 w-50">
<h1 class="text-center mb-4">Restaurants🥙</h1>

<ul>
<% @restaurants.each do |r| %>
  <li><%= link_to r.name, restaurant_path(r)%></li>
<% end %>
</ul>
<%= link_to "Add a new Restaurant", new_restaurant_path, class: 'btn btn-dark my-3' %>
</div>
